<p style="color: green"><%= notice %></p>
<div class="information-block">
  <h5 class="surf">We are thrilled you’re able to attend and can’t wait to celebrate with you. Please will you fill out the below table for yourself and
  anyone else on your invitation. At the end of the night there will be a limited number of taxis, so we will try to help people who are staying in the area coordinate their travel.</h4></h5>

  <div id="rsvps" class="rice">
        <table>
          <tr>
          <% if @rsvps.count == 0 %>
          <% else %>
            <th class="cookies"><div class="surf">Name </div></th>
            <th class="cookies"><div class="surf">Dietry Req.</div></th>
            <th class="cookies"><div class="surf">Accomodation</div></th>
            <th class="cookies"> </th>
          <% end %>
          </tr>
          <% @rsvps.each do |form| %>
            <tr>
              <td class="cookies"><div class="surf"><%= form.name %></div></td>
              <td class="cookies"><div class="surf"><%= form.food_requests %></div></td>
              <td class="cookies"><div class="surf"><%= form.accomodation %></div></td>
              <td class="cookies"><div class="surf"><%= link_to "Edit", edit_rsvp_path(form) %></td>
            </tr>
          <% end %><br>
        </table>
  </div>


<%= render "form" %>
</div>
